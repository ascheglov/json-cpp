// JSON for C++ : implementation, misc. utility classes
// Belongs to the public domain

#pragma once

#include <type_traits>

// TBD: move to the "json-cpp/" directory, since it doesn't use the `details` namespace

namespace jsoncpp
{
//     template<bool B, class T = void>
//     using enable_if_t = typename std::enable_if<B, T>::type;
}